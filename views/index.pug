extends layout.pug
block main
  //- h2.text-center= Title

  //-cards for different features

  .container.mt-5
    include includes/pop_up.pug
    +pop_up(title, desc, search)
    //- .row.justify-content-center
    //-   //- .card.mt-5(style={width: '75em', height: '25em'})
    //-   .card(style={width: '50em', height: '30em'})
    //-     #map
    //-       include includes/maps.pug
